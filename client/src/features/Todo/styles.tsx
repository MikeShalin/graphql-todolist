const list = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
}

const content = {
  display: 'flex',
  alignItems: 'center',
  width: '100%',
  cursor: 'pointer',
}

const name = { position: 'relative' }

export const styles = {
  list,
  content,
  name,
}
