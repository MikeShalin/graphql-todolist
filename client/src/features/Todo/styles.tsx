const list = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
}

const content = {
  display: 'flex',
  alignItems: 'center',
  width: '100%',
  cursor: 'pointer',
}

const label = {
  position: 'absolute',
  top: '50%',
  left: 'calc(50% - 30px)'
}

const name = { position: 'relative' }

export const styles = {
  list,
  content,
  label,
  name,
}
